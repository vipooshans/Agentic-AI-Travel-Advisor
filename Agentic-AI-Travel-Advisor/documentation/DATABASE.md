# Database Documentation

## Overview

PostgreSQL database for the Travel Advisor platform.

## Schema

Schema definitions live in `database/schemas/`.

## Migrations

Migration scripts live in `database/migrations/`.

## Seed Data

Initial seed data lives in `database/seed/`.
